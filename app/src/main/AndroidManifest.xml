<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fly.viewdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:name=".MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".path.PathActivity"/>
        <activity android:name=".taiji.TaiJiActivity"/>
        <activity android:name=".test.TestActivity"/>
        <activity android:name=".picture.PictureActivity"/>
        <activity android:name=".arc.ArcActivity"/>
        <activity android:name=".practice1.Practice1Activity"/>
        <activity android:name=".practice2.PracticeActivity2"/>
    </application>

</manifest>